node_modules
npm-debug.log
.DS_Store

.idea

example/example.js
example/example.js.map
demo/
styles/css/
yarn.lock

# we want build/ to be in the gitignore, but can't while Thread depends on this
# as a GitHub dependency
# build/
